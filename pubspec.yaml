name: rtx_robot
description: RTX ROBOT Floating Trading Signal App
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_overlay_window: ^0.4.3
  shared_preferences: ^2.2.2
  http: ^1.2.0
  google_generative_ai: ^0.4.6
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
